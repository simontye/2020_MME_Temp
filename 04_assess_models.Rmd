---
title: "assess models"
author: "fishkill friends"
date: "2021_03_04"
output: github_document
---

### Background

Candidate models are compared in terms of their coefficients to check for sensitivity of the coefficients to modeling choices, and their out-of-sample predictive performance, assessed using logloss and ROC-AUC. This is the same workflow as Till et al. (2019) except that I switched the file conversion from tibble to dataframe because of parsing errors.

### Step 1: Load packages and data

```{r: Load packages and data}
# Load packages
library(tidyverse)
library(caret)
library(glmnet)
library(Matrix)
library(e1071)
library(broom)
library(yardstick)
library(purrr)
library(data.table)

# Reset global environment
rm(list = ls())

# Set working directory
setwd("/Users/simontye/Documents/Research/Projects/MME_Temp/2020_MME_Temp/data/models")

# Load data
training      <- read.csv(file = "training.csv", head = TRUE, sep = ",")
testing       <- read.csv(file = "testing.csv",  head = TRUE, sep = ",")
```

### Step 2: Verify formatting

```{r: Verify formatting}
# Reformat training data
training <- training %>%
  mutate(year         = as.character(year),
         month        = as.character(month),
         season       = as.factor(season),
         summerkill   = as.factor(summerkill),
         lat          = as.numeric(lat),
         long         = as.numeric(long),
         population   = as.numeric(population),
         state        = as.factor(state),
         l1_code      = as.factor(l1_code),
         mean_bot_z   = as.numeric(mean_bot_z),
         mean_surf_z  = as.numeric(mean_surf_z),
         water_pca    = as.numeric(water_pca),
         mean_air_z   = as.numeric(mean_air_z),
         air_pca      = as.numeric(air_pca))

# Reformat testing data
testing <- testing %>%
  mutate(year         = as.character(year),
         month        = as.character(month),
         season       = as.factor(season),
         summerkill   = as.factor(summerkill),
         lat          = as.numeric(lat),
         long         = as.numeric(long),
         population   = as.numeric(population),
         state        = as.factor(state),
         l1_code      = as.factor(l1_code),
         mean_bot_z   = as.numeric(mean_bot_z),
         mean_surf_z  = as.numeric(mean_surf_z),
         water_pca    = as.numeric(water_pca),
         mean_air_z   = as.numeric(mean_air_z),
         air_pca      = as.numeric(air_pca))
```

### Step 2: Load models

```{r: Load models}
# Change working directory
setwd("/Users/simontye/Documents/Research/Projects/MME_Temp/2020_MME_Temp/data/models/assessment") 

# Save file names
model_names <- list.files()

# Load models
all_models <- model_names %>%
  map(., ~read_rds(.))

# Extract model names and object types
all_models <- tibble(name = str_sub(str_extract(model_names, "^.*\\."),
                                    1, -2), model = all_models)
```

### Step 3: Extract fitted values

Extract fitted values and append as new column.

```{r: Extract fitted values}
# Function to compare fitted values on testing data
predict_route <- function(model, newdata) {
  if("train" %in% class(model)) {
    predict(model, newdata, type = "prob")$pos
  } else {predict(model, newdata, type = "response")}
}

# Add comparison results
all_models <- all_models %>%
  mutate(fit_test = map(model, predict_route, newdata = testing))

#Warning messages:
#1: Problem with `mutate()` input `fit_test`.
#ℹ prediction from a rank-deficient fit may be misleading
#ℹ Input `fit_test` is `map(model, predict_route, newdata = testing)`. 
#2: Problem with `mutate()` input `fit_test`.
#ℹ prediction from a rank-deficient fit may be misleading
#ℹ Input `fit_test` is `map(model, predict_route, newdata = testing)`.

# Calculate logloss and auc
auc     <- rep(NA, nrow(all_models))
logloss <- auc

for (i in 1:nrow(all_models)) {
  a <- tibble(fit_test = all_models$fit_test[[i]],
              truth    = testing$summerkill)
  auc[i] <- roc_auc(a, truth, fit_test)$.estimate
  logloss[i] <- mn_log_loss(a, truth, fit_test)$.estimate
}

# Add logloss and auc
all_model_tests <- all_models %>%
  add_column(auc) %>%
  add_column(logloss) %>%
  arrange(logloss, desc(auc))

# Print logloss and auc in increasing order
print(tibble::as.tibble(all_model_tests), n = 24)
```

### Step 4: Extract coefficients

Extract coefficients from each model.

```{r: Extract coefficients}
# Function for extracting coefficients from different model types
extract_coef <- function(model, name) {
  if ("train" %in% class(model)) {
    m <- model$finalModel %>%
      coef(model$bestTune$lambda)
    out <- tibble(term = row.names(m),
                  estimate = m[, 1]) %>%
      filter(term != "(Intercept)") %>%
      dplyr::select(term, estimate)
  }
  if ("glmerMod" %in% class(model)) {
    out <- model %>%
      tidy() %>%
      filter(group == "fixed") %>%
      filter(term != "(Intercept)") %>%
      filter(p.value <= .3) %>%
      dplyr::select(term, estimate)
  }
  if ("glm" %in% class(model)) {
    out <- model %>%
      tidy() %>%
      filter(term != "(Intercept)") %>%
      dplyr::select(term, estimate)
  }
  out
}

# Add coefficients
all_models <- all_models %>%
  mutate(coef = map2(model, name, extract_coef))

# Subset simple air models
a1_models <- all_models[grep("a1", x = all_models$name), ]

# Subset simple water models
w1_models <- all_models[grep("w1", x = all_models$name), ]

# Subset complex air models
a2_models <- all_models[grep("a2", x = all_models$name), ]

# Subset complex water models
w2_models <- all_models[grep("w2", x = all_models$name), ]
```

### Step 5: Plot coefficients

Plot coefficients.

```{r: Plot coefficients}
# Change working directory
setwd("/Users/simontye/Documents/Research/Projects/MME_Temp/2020_MME_Temp/data/figures")

##################

# Describe PDF output
pdf(file = "models_a1.pdf", width  = 7, height = 7)

# Plot coefficents of simple air models
a1_models %>%
  unnest(coef) %>%
  ggplot(aes(x = fct_reorder(term, estimate), 
             y = estimate, 
             fill = estimate > 0)) +
  geom_col(show.legend = FALSE) +
  facet_wrap(~ name, ncol = 2) +
  coord_flip() +
  labs(x = NULL) +
  theme_bw()

# Close plot
dev.off()

##################

# Describe PDF output
pdf(file = "models_w1.pdf", width  = 7, height = 7)

# Plot coefficents
w1_models %>%
  unnest(coef) %>%
  ggplot(aes(x = fct_reorder(term, estimate), 
             y = estimate, 
             fill = estimate > 0)) +
  geom_col(show.legend = FALSE) +
  facet_wrap(~ name, ncol = 2) +
  coord_flip() +
  labs(x = NULL) +
  theme_bw()

# Close plot
dev.off()

##################

# Describe PDF output
pdf(file = "models_a2.pdf", width  = 7, height = 7)

# Plot coefficents of simple air models
a2_models %>%
  unnest(coef) %>%
  ggplot(aes(x = fct_reorder(term, estimate), 
             y = estimate, 
             fill = estimate > 0)) +
  geom_col(show.legend = FALSE) +
  facet_wrap(~ name, ncol = 2) +
  coord_flip() +
  labs(x = NULL) +
  theme_bw()

# Close plot
dev.off()

##################

# Describe PDF output
pdf(file = "models_w2.pdf", width  = 12, height = 12)

# Plot coefficents of simple air models
w2_models %>%
  unnest(coef) %>%
  ggplot(aes(x = fct_reorder(term, estimate), 
             y = estimate, 
             fill = estimate > 0)) +
  geom_col(show.legend = FALSE) +
  facet_wrap(~ name, ncol = 2) +
  coord_flip() +
  labs(x = NULL) +
  theme_bw()

# Close plot
dev.off()
```

######################################
Proceed to `05_fit_full_models.Rmd`
######################################
